# Ricchi-room架构设计
Ricchi-room是一款日本麻将（立直麻将）的多人牌桌记分微信小程序

## 技术栈
前端：WXSS，WXML
后端：Golang（GIN），port=8080
日志：zap
配置：viper
部署：云服务器单机部署

## 功能设计

### 牌桌设计
1. 一个牌桌最多支持4个微信用户接入，全局服务器同时最多支持100个牌桌，每个牌桌有自己的ID
2. 支持用户通过小程序分享链接进入，输入牌桌ID进入房间
3. 支持用户创建牌桌，生成全局唯一的牌桌ID
4. 牌桌ID需要4位数字随机，避免太过简单，但要保证全局不重复
5. 当所有人离开牌桌（断开链接）时，判定为牌桌解散，解散后回收牌桌ID
6. 进入牌桌后，可以选择离开牌桌，或者点击另外一名玩家的头像，向其支付虚拟的点数。
7. 创建房间的用户，作为房主，可以在任意时间点点击结算按钮执行结算动作。结算后，所有用户不会立即离开房间，并会收到小程序发来的结算结果消息推送。房主在这时可以选择解散房间或者再来一局。如果选择解散房间，则所有人强制断开连接。

### 支付设计
1. 每个人在进入牌桌后默认给予25000点数
2. 点数如果低于0，则认为被击飞，立即进行结算并推送，由房主决定解散房间或者再来一局
3. 支付的点数必须是100的倍数
4. 结算时根据四个人的最终点数，结算一二三四位

### 缓存设计
1. 在牌局从创建到解散的生命周期内，缓存所有数据到服务器的本地存储。牌局解散后，销毁当前牌局相关的所有数据。
2. 用户在重连到一个牌桌时，从服务器本地存储中恢复数据
3. 服务整体无状态，不存储用户历史的对局记录。历史记录通过微信小程序的结果推送，存储在用户本地的空间

### UI设计
1. 配色主要由 #c6ffdd, #fbd786, #f7797d 构成，可以进行三色渐变或者自由组合
2. UI元素以平滑简约为主

## 测试
前端通过vscode的vscode-wechat插件测试效果，后端可以本地启动服务